# frontend
## Prerequisites
### Node.js
- Node.js 18+
- npm
version check
```
node -v
npm -v
```
install node
```
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
```
## Start
```
cd frontend
npm install
npm run dev
```
